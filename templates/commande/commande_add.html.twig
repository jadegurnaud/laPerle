{% extends 'base.html.twig' %}

{% block title %}Nouvelle Commande
{% endblock %}

{% block body %}
	<div class="formadd">
		<h1>Ajout d'une Commande</h1>

		        {{ form_start(form) }}
        {{ form_row(form.total) }}
        {{ form_row(form.client_id) }}

        {# Render the produits field as a checkbox #}
        {{ form_label(form.produits) }}
        {{ form_errors(form.produits) }}
        {{ form_widget(form.produits, {'attr': {'type': 'checkbox'}}) }}
		<button class="btn">{{ button_label|default('Sauvegarder') }}</button>
	
		{{ form_end(form) }}

		<a href="{{ path('commande') }}">Retour à la liste</a>
	</div>
{% endblock %}
