{% extends 'base.html.twig' %}
 
{% block title %}Modifier Commande{% endblock %}
 
{% block body %}
<div class="formadd">
<h1>Modifier Commande</h1>
 
    {{ form_start(form) }}
          {{ form_row(form.total) }}
        {{ form_row(form.client_id) }}
        {{ form_row(form.livraison_id)}}
        {{ form_row(form.paiement) }}
    <button class="btn">{{ button_label|default('Sauvegarder') }}</button>
    {{ form_end(form) }}
    
    <a href="{{ path('commande') }}">Retour à la liste</a>
</div>
{% endblock %}